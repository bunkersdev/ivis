
<workflow-app name="WF_MOSC_[[WF_ENV]]_IVIS_SAP_LND_BD_10" xmlns="uri:oozie:workflow:0.5">
    <start to="LOAD_ONHAND_MTHLY"/>
    <kill name="Kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <action name="LOAD_ONHAND_MTHLY">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>transfer_lnd_inv_onhand_mthly.sh</exec>
              <argument>${WF_DATA_DIR}</argument>
            <file>${WF_APP_DIR}/ivis/scripts/shell/landing/transfer_lnd_inv_onhand_mthly.sh#transfer_lnd_inv_onhand_mthly.sh</file>
              <capture-output/>
        </shell>
        <ok to="End"/>
        <error to="JOB_FAILURE_EMAIL"/>
    </action>
    <action name="JOB_FAILURE_EMAIL">
             <email xmlns="uri:oozie:email-action:0.1">
            <to>${jobFailureEmails}</to>
            <subject>${cluster_name} - JOB FAILURE ${wf:name()}</subject>
            <body>Error message is: [${wf:errorMessage(wf:lastErrorNode())}]
Oozie Workflow Name: ${wf:name()}
External Job Id is ${wf:actionExternalId(wf:lastErrorNode())}
This is an automatic e-mail generated by the job.</body>
        </email>
        <ok to="Kill"/>
        <error to="Kill"/>
    </action>
    <end name="End"/>
</workflow-app>
